export const VERSION = '0.1.0'

export const HELP = `Session Assistant voice commands

Discussion and drafting:
  discuss — speak naturally without changing the draft.
  draft — dictate or request an edit.
  finalize — say “整理成最终稿” to polish and mark it ready.
  Speak naturally to discuss without changing the draft.
  Dictate or request an edit to replace the working draft.
  Say “整理成最终稿” to polish the draft and mark it ready.

Primary Agent handoff:
  Say “提交给 Agent” only when the final request should be sent and executed.

Connection:
  end — say “结束语音” to close without submitting.
  Say “结束语音” to close without submitting.

The voice model cannot execute work itself and cannot access Agent tools or credentials.`
