# Session product layer. Host settings and profile registration consume the
# shared Settings and realtime model runtime; the Web half requires the
# provider-neutral voiceAgent Client service from dsh-realtime-voice.
- insert:
    - id: dsh-session-assistant
      name: 'dsh-session-assistant'
