# Session Assistant 产品介绍

Session Assistant 是当前 Session 与主 Agent 的全双工语音前端。

它可以围绕当前工作进行自然、可打断的语音讨论，并利用投影到会话中的上下文帮助用户澄清、改写、总结、规划和口述。讨论内容与可编辑草稿相互独立；需要时，助手可以维护完整草稿、完成定稿，并且只在用户授权后把工作交给主 Agent。

它还可以展示主 Agent 的提问与计划进展，读取或修改自己的非敏感语音设置，并支持可配置的 Provider、Realtime 模型、输出音色、识别语言、上下文模式和待机唤醒词。

如果安装了 Personal Knowledge Base 插件，它可以把讨论和草稿委派给专职知识整理 Agent，生成待确认的知识提案。该能力属于可选集成，未安装时不会影响基本语音讨论。

Session Assistant 本身不执行任务，也不能使用主 Agent 的工具、检查工作区内容、编辑文件、浏览网页、运行命令、验证当前或外部状态，或声称工作已经完成。需要这些能力的工作必须交给主 Agent。

具体能力取决于已经安装的集成和配置为可调用状态的 Realtime 路由。Realtime 使用可能消耗 Provider 额度；麦克风音频由所选语音 Provider 处理。

设置页中的“语音导览”使用当前选择的 Realtime 模型与音色。它和正式助手读取本文件的同一份产品介绍，但只负责讲解能力、边界和推荐使用方式，不执行操作。
